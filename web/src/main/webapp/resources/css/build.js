/* Special CSS-only build file */
({
    appDir: ".",
    baseUrl: ".",
    removeCombined: true,
    preserveLicenseComments: false,
    //dir: "${project.build.directory}/${project.build.finalName}/resources",

    optimizeCss: "standard",
    cssImportIgnore: null,

    paths: {
    }
})
